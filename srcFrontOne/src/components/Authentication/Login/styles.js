import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    rootLogin: {
        alignItems: 'center',
    },
    logo: {
        width: '70%',
        maxWidth: 300,
        maxHeight: 300,
        padding: 20,
    },
})


export default styles;